/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['icon-library.com', 'firebasestorage.googleapis.com', 'drive.google.com'],
    },
}

module.exports = nextConfig
